---
# maintains file name, binary names, and anything which is not a directory or path

# Environment file names
stats_agent_environment_general_file_name: "{{ service_identifier }}.env"
stats_agent_environment_secrets_file_name: "{{ service_identifier }}.secrets.env"
